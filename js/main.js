// Generated by CoffeeScript 1.7.1
(function() {
  var Item, ItemView, Items, ItemsView, LoginView, MainView, Router, User,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  $(function() {
    var mainView, router, userModel, view;
    userModel = new User;
    mainView = new MainView({
      model: userModel
    });
    mainView.render();
    view = null;
    router = new Router;
    router.on('route:items', function() {
      userModel.set({
        logged: true
      });
      if (view) {
        view.undelegateEvents();
      }
      view = new ItemsView;
      return view.render();
    });
    router.on('route:item', function(id) {
      userModel.set({
        logged: true
      });
      if (view) {
        view.undelegateEvents();
      }
      view = new ItemView({
        model: new Item({
          id: id
        })
      });
      return view.render();
    });
    router.on('route:login', function() {
      userModel.set({
        logged: false
      });
      if (view) {
        view.undelegateEvents();
      }
      view = new LoginView;
      return view.render();
    });
    return Backbone.history.start();
  });

  Router = (function(_super) {
    __extends(Router, _super);

    function Router() {
      return Router.__super__.constructor.apply(this, arguments);
    }

    Router.prototype.routes = {
      '': 'items',
      'item/:id': 'item',
      'login': 'login'
    };

    Router.prototype.initialize = function() {
      return $.ajaxSetup({
        statusCode: {
          401: function() {
            return window.location.replace('/#login');
          }
        }
      });
    };

    return Router;

  })(Backbone.Router);

  Items = (function(_super) {
    __extends(Items, _super);

    function Items() {
      return Items.__super__.constructor.apply(this, arguments);
    }

    Items.prototype.url = 'items';

    return Items;

  })(Backbone.Collection);

  Item = (function(_super) {
    __extends(Item, _super);

    function Item() {
      return Item.__super__.constructor.apply(this, arguments);
    }

    Item.prototype.defaults = {
      name: 'default'
    };

    Item.prototype.urlRoot = 'items';

    return Item;

  })(Backbone.Model);

  ItemView = (function(_super) {
    __extends(ItemView, _super);

    function ItemView() {
      this.render = __bind(this.render, this);
      return ItemView.__super__.constructor.apply(this, arguments);
    }

    ItemView.prototype.el = '#content';

    ItemView.prototype.events = {
      'click #save': 'save'
    };

    ItemView.prototype.template = "name:\n<input id=\"name\" type=\"text\" value=\"{{name}}\"/>\n<button id=\"save\" type=\"button\" class=\"btn btn-default btn-xs\">Save</button>";

    ItemView.prototype.initialize = function() {
      this.model.on('change', this.render);
      return this.model.fetch();
    };

    ItemView.prototype.render = function() {
      this.$el.html(Mustache.render(this.template, this.model.toJSON()));
      return this;
    };

    ItemView.prototype.save = function(event) {
      this.model.set({
        name: this.$('#name').val()
      });
      this.model.save();
      return window.location.replace('/#');
    };

    return ItemView;

  })(Backbone.View);

  ItemsView = (function(_super) {
    __extends(ItemsView, _super);

    function ItemsView() {
      this.render = __bind(this.render, this);
      return ItemsView.__super__.constructor.apply(this, arguments);
    }

    ItemsView.prototype.el = '#content';

    ItemsView.prototype.collection = new Items;

    ItemsView.prototype.events = {
      'click #add': 'add',
      'click .edit': 'edit',
      'click .delete': 'delete'
    };

    ItemsView.prototype.template = "<button id=\"add\" class=\"btn btn-default btn-xs\">Add</button>\n<ul>\n	{{#items}}\n		<li>\n			<div data-item=\"{{id}}\">\n				{{name}}\n				<button type=\"button\" class=\"btn btn-default btn-xs edit\">Edit</button>\n				<button type=\"button\" class=\"btn btn-default btn-xs delete\">Delete</button>\n			</div>\n		</li>\n	{{/items}}\n</ul>";

    ItemsView.prototype.initialize = function() {
      this.collection.on('reset add remove', this.render);
      return this.collection.fetch({
        reset: true
      });
    };

    ItemsView.prototype.render = function() {
      this.$el.html(Mustache.render(this.template, {
        items: this.collection.toJSON()
      }));
      return this;
    };

    ItemsView.prototype.add = function(event) {
      return this.collection.add(this.collection.create(new Item, {
        wait: true
      }));
    };

    ItemsView.prototype.edit = function(event) {
      var id;
      id = this.$(event.target).parent().data('item');
      return window.location.replace("/#item/" + id);
    };

    ItemsView.prototype["delete"] = function(event) {
      var item;
      item = new Item(this.collection.remove(this.collection.at(this.$(event.target).parent().data('item'))));
      return item.destroy();
    };

    return ItemsView;

  })(Backbone.View);

  LoginView = (function(_super) {
    __extends(LoginView, _super);

    function LoginView() {
      this.render = __bind(this.render, this);
      return LoginView.__super__.constructor.apply(this, arguments);
    }

    LoginView.prototype.el = '#content';

    LoginView.prototype.events = {
      'click #ok': 'ok'
    };

    LoginView.prototype.html = "login: <input id=\"login\" type=\"text\"/>\npass: <input id=\"pass\" type=\"password\"/>\n<button id=\"ok\" type=\"button\" class=\"btn btn-default btn-xs\">Ok</button>";

    LoginView.prototype.render = function() {
      this.$el.html(this.html);
      return this;
    };

    LoginView.prototype.ok = function(event) {
      var credentials;
      credentials = {
        login: this.$('#login').val(),
        pass: this.$('#pass').val()
      };
      return $.post('/login', credentials, function(data) {
        return window.location.replace('/#');
      });
    };

    return LoginView;

  })(Backbone.View);

  MainView = (function(_super) {
    __extends(MainView, _super);

    function MainView() {
      this.render = __bind(this.render, this);
      return MainView.__super__.constructor.apply(this, arguments);
    }

    MainView.prototype.el = 'body';

    MainView.prototype.events = {
      'click #logout': 'logout'
    };

    MainView.prototype.template = "{{#logged}}\n	<button id=\"logout\" class=\"btn btn-default btn-xs\">Logout</button>\n{{/logged}}\n<div id=\"content\">\n</div>";

    MainView.prototype.initialize = function() {
      return this.model.on('change', this.render);
    };

    MainView.prototype.render = function() {
      this.$el.html(Mustache.render(this.template, this.model.toJSON()));
      return this;
    };

    MainView.prototype.logout = function(event) {
      return $.get('/logout', function(data) {
        return window.location.replace('/#login');
      });
    };

    return MainView;

  })(Backbone.View);

  User = (function(_super) {
    __extends(User, _super);

    function User() {
      return User.__super__.constructor.apply(this, arguments);
    }

    User.prototype.defaults = {
      logged: true
    };

    return User;

  })(Backbone.Model);

}).call(this);
